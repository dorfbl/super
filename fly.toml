# fly.toml app configuration file generated for super-q-57og on 2026-05-06T06:41:50Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'super-q-57og'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[deploy]
  strategy = 'immediate'

[[mounts]]
  source = 'auth_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 512
